1. Open the .env file and add your OpenAI key.
2. Open a terminal in the folder containing the docker-compose.yml file (the campaign folder).
3. Run the command: docker-compose up --build.
4. Open http://localhost:3000/ in your browser.
5. View the results in the ./campaign/assets/output folder.